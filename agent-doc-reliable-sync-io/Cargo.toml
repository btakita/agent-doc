[package]
name = "agent-doc-reliable-sync-io"
version = "0.34.147"
edition = "2024"
description = "Unix-domain-socket carrier for lazily's reliable-sync plane (#lzsync) — implements IpcSink/IpcSource over agent-doc's existing controller socket (sidecar-retirement Phase 3A)"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"
publish = false

[lib]
name = "agent_doc_reliable_sync_io"

[dependencies]
agent-doc-hash = { path = "../agent-doc-hash", version = "0.34.147" }
agent-doc-ipc-io = { path = "../agent-doc-ipc-io", version = "0.34.147" }
agent-doc-merge = { path = "../agent-doc-merge", version = "0.34.147" }
anyhow = "1"
base64 = "0.22"
lazily = { version = "0.40.0", features = ["ipc", "ipc-msgpack", "serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
