[package]
name = "agent-doc-memory"
version = "0.34.66"
edition = "2024"
description = "Pure semantic memory ranking helpers for agent-doc"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"

[lib]
name = "agent_doc_memory"

[dependencies]
serde = { version = "1", features = ["derive"] }
tsift-memory = "0.1.70"
