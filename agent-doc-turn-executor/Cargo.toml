[package]
name = "agent-doc-turn-executor"
version = "0.35.109"
edition = "2024"
description = "Shared turn executor model vocabulary for agent-doc"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"
publish = false

[lib]
name = "agent_doc_turn_executor"

[dependencies]
agent-doc-frontmatter = { path = "../agent-doc-frontmatter", version = "0.35.109" }
agent-doc-hash = { path = "../agent-doc-hash", version = "0.35.109" }
anyhow = "1"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
