[package]
name = "agent-doc-lease-io"
version = "0.35.213"
edition = "2024"
description = "Lease-related process and filesystem adapters for agent-doc"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"
publish = false

[lib]
name = "agent_doc_lease_io"

[dependencies]
agent-doc-fs = { path = "../agent-doc-fs", version = "0.35.213" }
agent-doc-lease = { path = "../agent-doc-lease", version = "0.35.213" }

[dev-dependencies]
tempfile = "3"
