[package]
name = "agent-doc-tmux-commands"
version = "0.34.65"
edition = "2024"
description = "Pure tmux command builders and parsers for agent-doc"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"

[lib]
name = "agent_doc_tmux_commands"

[dependencies]
serde = { version = "1", features = ["derive"] }
