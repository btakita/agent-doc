[package]
name = "agent-doc-workflow"
version = "0.34.65"
edition = "2024"
description = "Pure workflow evidence-to-decision kernel for agent-doc"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"

[lib]
name = "agent_doc_workflow"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
