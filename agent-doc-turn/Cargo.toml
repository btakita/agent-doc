[package]
name = "agent-doc-turn"
version = "0.34.65"
edition = "2024"
description = "Pure turn lifecycle state machine for agent-doc"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"

[lib]
name = "agent_doc_turn"

[dependencies]
lazily = "0.13.1"
serde = { version = "1", features = ["derive"] }
