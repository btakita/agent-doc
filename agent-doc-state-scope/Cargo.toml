[package]
name = "agent-doc-state-scope"
version = "0.35.279"
edition = "2024"
description = "Lifetime-typed lazily scopes (#stategraphjoin) shared across agent-doc crates"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"
publish = false

[lib]
name = "agent_doc_state_scope"

[dependencies]
lazily = { version = "0.56.1", features = ["thread-safe"] }
