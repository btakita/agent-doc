[package]
name = "agent-doc-tmux"
version = "0.35.78"
edition = "2024"
description = "Pure tmux realtime model state for agent-doc"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"
publish = false

[lib]
name = "agent_doc_tmux"

[dependencies]
agent-doc-state-scope = { path = "../agent-doc-state-scope", version = "0.35.78" }
lazily = { version = "0.53.0", features = ["thread-safe"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
