[package]
name = "agent-doc-sync"
version = "0.35.30"
edition = "2024"
description = "Pure sync scope and layout-state planning policy for agent-doc"
license = "MIT"
repository = "https://github.com/btakita/agent-doc"
publish = false

[lib]
name = "agent_doc_sync"

[dependencies]
agent-doc-fs = { path = "../agent-doc-fs", version = "0.35.30" }
agent-doc-harness = { path = "../agent-doc-harness", version = "0.35.30" }
agent-doc-turn = { path = "../agent-doc-turn", version = "0.35.30" }
anyhow = "1"

[dev-dependencies]
tempfile = "3"
